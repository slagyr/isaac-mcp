name: CI Tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      NO_COLOR: "1"
    steps:
      - name: Check out isaac-mcp
        uses: actions/checkout@v4

      - name: Check out isaac (fixture modules)
        run: git clone --depth 1 https://github.com/slagyr/isaac.git ../isaac

      - name: Check out isaac-foundation
        run: git clone --depth 1 https://github.com/slagyr/isaac-foundation.git ../isaac-foundation

      - name: Check out isaac-agent
        run: git clone --depth 1 https://github.com/slagyr/isaac-agent.git ../isaac-agent

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Clojure and Babashka
        uses: DeLaGuardo/setup-clojure@13.2
        with:
          bb: latest
          cli: latest

      - name: Cache Clojure dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gitlibs
            ~/.deps.clj
            .cpcache
          key: ${{ runner.os }}-clojure-mcp-${{ hashFiles('**/deps.edn', '**/bb.edn') }}
          restore-keys: |
            ${{ runner.os }}-clojure-mcp-

      - name: Run bb ci
        run: bb ci
